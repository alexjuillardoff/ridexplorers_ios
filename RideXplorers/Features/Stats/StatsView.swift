import SwiftUI

struct StatsView: View {
    /// Onglet "Stats" (placeholder pour statistiques/analyses à venir).
    var body: some View {
        PageView(title: "Stats") {
            Spacer()
        }
    }
}

struct StatsView_Previews: PreviewProvider {
    static var previews: some View {
        StatsView()
    }
}

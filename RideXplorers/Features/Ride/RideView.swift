import SwiftUI

struct RideView: View {
    /// Onglet "Ride" (placeholder pour les fonctionnalités à venir).
    var body: some View {
        PageView(title: "Ride") {
            Spacer()
        }
    }
}

struct RideView_Previews: PreviewProvider {
    static var previews: some View {
        RideView()
    }
}

import SwiftUI

struct RideView: View {
    var body: some View {
        PageView(title: "Ride") {
            Spacer()
        }
    }
}

struct RideView_Previews: PreviewProvider {
    static var previews: some View {
        RideView()
    }
}
